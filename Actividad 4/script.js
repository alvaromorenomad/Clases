'use strict'



/* function crearContenedor(nodoSalida){
    let contador = 0;

    return function () {
        contador++;
        nodoSalida.innerText = contador;
    }
}

const nodoBtnMasIfhone = document.querySelector('#btnMasIfhone');
const nodoCantidad = document.querySelector('#numeroCantidad');


const nodoBtnMasCargador = document.querySelector('#btnMasCargador');
const nodoNumeroCantidad2 = document.getElementById('numeroCantidad2');

const nodoBtnMasFunda = document.querySelector('#btnMasFunda');
const nodoNumeroCantidad3 = document.querySelector('#numeroCantidad3');


const sumarIphone = crearContenedor(nodoCantidad);
const sumarCargador = crearContenedor(nodoNumeroCantidad2);
const sumarFunda = crearContenedor(nodoNumeroCantidad3);

nodoBtnMasCargador.addEventListener ('click', sumarCargador);
nodoBtnMasIfhone.addEventListener('click', sumarIphone);
nodoBtnMasFunda.addEventListener('click', sumarFunda);

function disminuirContenedor(nodoSalida){
    
    let contador = Number(nodoSalida.innerText);

    return function () {
        if(contador > 0){
            contador--;
            nodoSalida.innerText = contador;
        };
    }
}

const nodoBtbMenosIfhone = document.getElementById('btnMenosIfhone');
const nodoBtnMenosCargador = document.getElementById('btnMenosCargador');
const nodoBtnMenosFunda = document.getElementById('btnMenosFunda');

const restarIphone = disminuirContenedor(nodoCantidad);
const restarCargador = disminuirContenedor(nodoNumeroCantidad2);
const restarFunda = disminuirContenedor(nodoNumeroCantidad3);

nodoBtbMenosIfhone.addEventListener('click', restarIphone);
nodoBtnMenosCargador.addEventListener('click', restarCargador);
nodoBtnMenosFunda.addEventListener('click', restarFunda);

 */

